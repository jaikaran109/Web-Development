const { log } = require('console');
const os = require('os');

console.log(os.freemem());  // free memory in system

console.log(os.homedir()); // home directory

console.log(os.hostname()); // system hostname - jaikaran109-LOQ

console.log(os.platform()); // platform 

console.log(os.release()); // release version

console.log(os.totalmem()); // total memory in system

console.log(os.uptime()); // system kbse chalu rha




